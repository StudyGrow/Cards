mutation CastVote($cardId: String!, $value: Float!) {
  castVote(data: { cardId: $cardId, value: $value }) {
    id
    user {
      id
    }
    card {
      id
    }
    lecture {
      id
    }
    value
  }
}
