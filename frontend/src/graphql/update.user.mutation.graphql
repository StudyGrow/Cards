mutation UpdateUser($username: String, $firstName: String, $lastName: String) {
  updateUser(data: { username: $username, firstName: $firstName, lastName: $lastName }) {
    id
    username
    creationDate
    name
    surname
    status
    confirmed
  }
}
