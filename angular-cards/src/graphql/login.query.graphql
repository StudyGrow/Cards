query Login($username: String!, $password: String!) {
  login(data: { username: $username, password: $password }) {
    _id
    role
    username
    email
    creationDate
    name
    surname
    status
    confirmed
  }
}
